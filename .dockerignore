# Secrets never enter the build context (the image gets its config from the runtime environment).
**/.env
**/.env.*
!**/.env.example

# Dependencies and build output are rebuilt inside the image.
**/node_modules
**/.next
**/coverage

# Local data and logs
**/uploads
**/*.log
app.db
instance

# Tooling and VCS
.git
.github
.vscode
.cursor
.claude
.venv

# Not part of the unified app (Backend/ is the pre-move copy with local secrets)
_archive
Backend
backend
frontend
FutureEnhancement
agronexus-springboot
GPS
Nonfunctionalrequirements
