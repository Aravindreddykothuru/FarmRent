# Secrets never enter the build context (the image gets its config from the runtime environment).
**/.env
**/.env.*
!**/.env.example

# Dependencies and build output are rebuilt inside the image.
**/node_modules
**/.next
**/coverage

# Local data and logs
**/uploads
**/*.log
app.db
instance

# Tooling and VCS
.git
.github
.vscode
.cursor
.claude
.venv

# Local leftovers of removed prototypes and old copies — untracked, but some hold local secrets
_archive
scratch
Backend
backend
frontend
FutureEnhancement
agronexus-springboot
GPS
Nonfunctionalrequirements
nginx
